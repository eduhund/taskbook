const { paramsProcessor } = require("../src/utils/validate");

/*
describe("Valid params", () => {
	test.each([
		{
			name: "auth",
			dataFile: "paramsValidator/auth.json",
			result: {},
		},
	])("Try to auth — $name", async ({ dataFile, result }) => {
		var data = readFileAsObject(dataFile);

		const response = await getApiRequest("auth", data);
		expect(paramsProcessor()).toEqual(result);
	});
});
*/
